
CREATE TABLE `properties` (
    `id` INTEGER PRIMARY KEY AUTO_INCREMENT,
    `type` VARCHAR,
    `location` VARCHAR,
    `bedrooms` INTEGER,
    `bathrooms` INTEGER,
    `price` INTEGER,
    `imageUrl` VARCHAR
);